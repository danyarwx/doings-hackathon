def check_hardware_warnings():
    print("🎙️  [Capture] Windows detected. pywhispercpp will run on CPU unless configured with CUDA.")
    print("💡 [Tip] If transcription is slow, try running with '--model small'.")